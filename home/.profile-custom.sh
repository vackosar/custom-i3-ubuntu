# Gnome keyring setup for SSH
if [ "ubuntu" = "i3" ];then
    eval SSH_AUTH_SOCK=/run/user/1000/keyring/ssh
    export SSH_AUTH_SOCK
fi
# Gnome keyring setup for SSH
if [ "ubuntu" = "i3" ];then
    eval SSH_AUTH_SOCK=/run/user/1000/keyring/ssh
    export SSH_AUTH_SOCK
fi
